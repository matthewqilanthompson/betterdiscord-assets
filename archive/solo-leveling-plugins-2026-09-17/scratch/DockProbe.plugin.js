/**
 * @name DockProbe
 * @description Inert stub. The diagnostic probe is retired — this file only exists so the BetterDiscord-folder symlink resolves (a dangling symlink kills BD's entire addon scan). Safe to delete AFTER removing the symlink in the BD plugins folder.
 * @version 2.0.0
 * @author matthewthompson
 */
"use strict";

module.exports = class DockProbe {
  start() {}
  stop() {}
};
